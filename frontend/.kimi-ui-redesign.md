Redesign the main menu, mission select, and battle HUD using the new Theme.ts design system.

Phase 1: Redesign src/scenes/BootScene.ts main menu
- Use COLORS and FONTS from Theme.ts
- Title: larger font (FONTS.title.sizes.xl), add subtle glow effect using a second text layer with blue color and lower alpha
- Background: gradient effect using two rectangles (darker top, lighter bottom)
- Buttons: use createButton() from Theme.ts
- Add version text at bottom: "v0.2.0" in muted color
- Keep existing continue/new game logic

Phase 2: Redesign src/scenes/MissionSelectScene.ts
- Use COLORS and FONTS from Theme.ts
- Cards: use createPanel() for background, add difficulty color strip on left side (3px wide rectangle)
- Card hover: change border color to accent blue on pointerover, restore on pointerout
- Title: use FONTS.title
- Lock overlay: semi-transparent dark rectangle over locked cards

Phase 3: Redesign src/scenes/BattleScene.ts HUD
- Use COLORS and FONTS from Theme.ts
- Action bar buttons: use createButton() from Theme.ts
- HUD text panels: use createPanel() backgrounds
- Turn indicator: styled panel with accent color

Phase 4: Redesign src/scenes/ResultScene.ts
- Use COLORS and FONTS from Theme.ts
- Reward panel: use createPanel()
- Stats display: clean layout with FONTS.body
- Return button: use createButton()

Run npm test and npm run build after all changes.
Reply with what was changed and validation results.
