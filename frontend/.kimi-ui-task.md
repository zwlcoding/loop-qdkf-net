Create src/ui/Theme.ts with a complete design system. Then update BootScene to preload all new sprite assets. Then update BattleScene to use sprites instead of text labels for units. Then update GridMap to use terrain tile sprites.

Phase 1: Create src/ui/Theme.ts
- Export COLORS object with bg, text, accent, border, terrain, difficulty, squad sub-objects
- Export FONTS object with title, body, ui, hud sub-objects
- Export createButton(scene, x, y, label, width, height, callback) helper that creates styled button with hover/click effects
- Export createPanel(scene, x, y, w, h, options) helper for styled panels

Phase 2: Update src/scenes/BootScene.ts
- Preload character sprites: vanguard-sprite.png, skirmisher-sprite.png, controller-sprite.png, support-sprite.png, caster-sprite.png from assets/units/
- Preload terrain sprites: tile-plain-sprite.png, tile-mountain-sprite.png, tile-urban-sprite.png, tile-forest-sprite.png, tile-water-sprite.png from assets/tiles/
- Use texture keys: sprite-vanguard, sprite-skirmisher, sprite-controller, sprite-support, sprite-caster, tile-plain-sprite, tile-mountain-sprite, tile-urban-sprite, tile-forest-sprite, tile-water-sprite

Phase 3: Update src/scenes/BattleScene.ts
- In createUnitsFromSetup, after creating each unit, add a sprite image at the unit position using the chassis sprite texture
- Keep existing HP bar and badge text on top of the sprite
- Sprite size should be about 48x48

Phase 4: Update src/core/GridMap.ts
- In the tile rendering loop, replace fillRect with drawImage using terrain sprite textures
- Use the terrain type to look up the correct sprite key

Run npm test and npm run build after all changes.
Reply with what was changed and validation results.
